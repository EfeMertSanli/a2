package parser;

/**
 * Enum representing the different command types.
 *
 * @author uuifx
 */
public enum CommandType {
    LOAD_DATABASE,
    QUIT,
    ADD,
    REMOVE,
    NODES,
    EDGES,
    RECOMMEND,
    EXPORT,
    UNKNOWN
}